# helm/values-staging.yaml — STAGING environment overrides
replicaCount: 2

env:
  SPRING_PROFILES_ACTIVE: "staging"
  LOG_LEVEL: "INFO"

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 1000m
    memory: 768Mi
