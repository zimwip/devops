<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.myorg</groupId>
  <artifactId>lib-platform</artifactId>
  <version>1.3.0</version>
  <packaging>jar</packaging>
  <name>Platform Library</name>
  <description>
    Cross-cutting platform library: security, logging, tracing, pub/sub stubs.
    Published to the local Nexus instance during testenv bootstrap so that
    service builds can resolve com.myorg:lib-platform without a real Artifactory.
  </description>

  <properties>
    <java.version>17</java.version>
    <maven.compiler.source>${java.version}</maven.compiler.source>
    <maven.compiler.target>${java.version}</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <!--
    Deployment target: Nexus hosted repository 'maven-internal'.
    create.sh calls: mvn deploy -s <settings> -DaltDeploymentRepository=...
    URL uses localhost so it works from the host during bootstrap.
  -->
  <distributionManagement>
    <repository>
      <id>nexus</id>
      <url>http://localhost:8081/repository/maven-internal/</url>
    </repository>
  </distributionManagement>

</project>
